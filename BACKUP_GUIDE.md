# 备份规范

本项目采用 Git 驱动的内容管理方式。代码、CMS 内容文件和图片均应随版本记录保存；不要把备份建立在个人电脑的唯一副本上。

## 需要备份的内容

| 内容 | 位置 | 推荐备份方式 |
| --- | --- | --- |
| 网站代码与配置 | `app/`、`src/`、根目录配置文件 | 推送至受团队管理的远程 Git 仓库 |
| CMS 内容 | `src/content/projects/`、`people/`、`stories/`、`achievements/` | 与代码在同一个提交中保存 |
| 图片与附件 | `public/images/`、`public/files/` | 与对应内容提交同时保存 |
| 运行配置 | Vercel 项目设置、域名与环境变量 | 由 Admin 在交接清单中记录变量名称和配置位置，不记录密钥值 |

## 日常备份流程

1. 在 CMS 或本地更新重要内容后，检查 `git status`，确认内容文件、图片和配置变更都已出现。
2. 在提交前运行 `npm run lint` 与 `npm run build`，并在预览环境检查新增页面。
3. 使用清晰的提交说明提交到 `dev`；审核通过后合并到 `main`。
4. 确认远程仓库已收到提交，并保留 Vercel 的成功部署记录。

每次学期内容集中更新、年度归档、域名或部署配置变更后都必须完成一次上述流程。

## 恢复原则

- 先从远程 Git 仓库新建恢复分支，在预览环境验证，再合并到 `main`；不要直接覆盖生产分支。
- 恢复图片或附件时，保持原始相对路径，避免破坏内容中的引用。
- 遇到误删或误发布时，优先使用 Git 历史还原对应文件，而不是删除历史记录。
- 如发现密钥、令牌或私密资料被提交，应立即在服务商后台撤销或轮换，并从后续提交中移除；不要仅依赖 `.gitignore` 修复已泄露的历史。

## 责任分工

Admin 负责远程仓库、Vercel、域名和环境变量的可恢复性；Editor 负责内容与图片在提交前的完整性；交接时按 [HANDOVER.md](HANDOVER.md) 与 [ROLE_GUIDE.md](ROLE_GUIDE.md) 核对权限和最近一次成功部署。
