# 图片优化规范

本项目通过 `next/image` 渲染站内图片；图片文件必须在提交前压缩。不要把相机原图、超大 JPG、PSD、AI、TIFF 或含敏感 EXIF 信息的文件放入 `public/images/`。通过 Decap CMS 上传的文件会按集合写入 `projects/`、`people/`、`stories/` 或 `achievements/` 子目录。

## 推荐规格

| 用途 | 最低建议 | 页面展示建议 |
| --- | --- | --- |
| 项目封面 | 1200 × 800 px | 16:10 时可使用 1600 × 1000 px |
| 人物头像 | 800 × 800 px | 人物卡片为 3:4，可使用 1200 × 1600 px 减少裁切 |
| 文章图片 | 宽度至少 1200 px | 文章封面建议 16:9 |
| 图集图片 | 宽度至少 1200 px | 4:3 最适合当前图集 |
| 默认分享图 | 1200 × 630 px | 使用 `public/images/og-image.svg` |

## 格式与大小

- 优先 WebP；图形和默认分享图可使用 SVG；透明图片使用 PNG。
- 封面和头像建议不超过 350 KB，图集单张不超过 500 KB；确有必要时保持在 800 KB 内。
- JPEG/WebP 通常使用 70–82 的质量值，先在手机网络下检查文字和人物细节。
- 删除 EXIF 中的位置、设备、作者等敏感信息后再导出网页版本。

## 检查方法

1. 使用文件管理器按大小排序 `public/images/`，优先处理超过 800 KB 的图片。
2. 使用页面实际比例预览图片，确认重要人物和文字不会被 `object-cover` 裁切。
3. 运行 `npm run build` 后检查图片路径没有导致构建或运行时错误。
4. 具体目录与命名规则见 `IMAGE_GUIDE.md`。
